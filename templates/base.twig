{% block html_head_container %}
	{% block head %}
		{% include 'html-header.twig' %}
	{% endblock %}
{% endblock %}

	<body class="{{ body_class }} {% if post.achtergrond == 'bg-brown' %}bg-brown text-white{% else %}bg-white{% endif %}" data-template="base.twig" data-scroll-container>	
		
		<a href="javascript:void(0)" class="btn-scrollup js-scrollup"><i class="fas fa-chevron-up"></i></a>
		
		{% block header %}
			{% include 'header.twig' %}
		{% endblock %}
		
		<main>
			{% if post.title %}
				<h1 class="d-none">{{ title }}</h1>
			{% endif %}
			{% block content %}
				Sorry, no content
			{% endblock %}
		</main>
		
		{% block footer %}

			{{ function('wp_footer') }}
		{% endblock %}
	</body>
</html>
